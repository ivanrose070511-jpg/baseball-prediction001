name = "dawn-hill-de60"
main = "src/worker.js"
compatibility_date = "2026-06-16"
workers_dev = true

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"

# Cron triggers are managed in the Cloudflare dashboard because the deployment
# API token is intentionally not allowed to edit account-level schedules.
